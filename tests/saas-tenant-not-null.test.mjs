import assert from'node:assert/strict';import fs from'node:fs';import path from'node:path';import test from'node:test';import{fileURLToPath}from'node:url';
const root=path.resolve(path.dirname(fileURLToPath(import.meta.url)),'..');const sql=fs.readFileSync(path.join(root,'migrations','0079_enforce_business_tenant_not_null.sql'),'utf8');const integration=fs.readFileSync(path.join(root,'migrations','tests','saas_business_tenant_not_null_integration.sql'),'utf8');
test('tenant business columns become NOT NULL only after a zero-null precondition',()=>{assert.match(sql,/WHERE organization_id IS NULL/);assert.match(sql,/IF missing_rows<>0/);assert.match(sql,/ALTER COLUMN organization_id SET NOT NULL/);assert.doesNotMatch(sql,/['"]audit_logs['"]|['"]activity_logs['"]/);});
test('staging matrix preserves the explicit platform audit exception',()=>{assert.match(integration,/all_33_business_columns_are_not_null/);assert.match(integration,/platform_audit_exception_remains_nullable/);});
